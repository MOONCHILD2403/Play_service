const {handlePostPreference,handleGetPreference} = require("./preferences");
const {handleSimilarProfiles} = require("./similar");

module.exports = {
    handlePostPreference,
    handleGetPreference,
    handleSimilarProfiles,
};